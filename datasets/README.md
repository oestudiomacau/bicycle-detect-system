# Anomalib 数据目录

训练页面默认读取下面的目录，也可以在 GUI 中选择任意其他目录：

```text
datasets/
└── parking/
    ├── normal/      # 正常停放图片，用于训练
    └── abnormal/    # 越界、倒地、堆放图片，仅用于测试，可不提供
```

建议准备至少 50 张固定机位、光照和清晰度接近的正常图片。没有异常图片时，程序会用 Anomalib 的合成异常完成测试集构造。

图片和训练产物默认不会提交到 Git，避免仓库体积过大或上传包含隐私的监控画面。
